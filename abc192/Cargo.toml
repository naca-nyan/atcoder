[package]
name = "abc192"
version = "0.1.0"
authors = ["naca-nyan <naca.nyan@gmail.com>"]
edition = "2018"

# dependencies added to new project
[dependencies]
proconio = "*"

[profile.release]
lto = true
panic = 'abort'
